const sliderContainer=document.querySelector(".slider"),sliderRange=document.querySelector(".slider__range");sliderRange.addEventListener("input",(e=>sliderContainer.style.setProperty("--curtain-place",`${e.target.value}%`)));